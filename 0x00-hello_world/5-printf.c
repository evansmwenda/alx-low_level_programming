#include <stdio.h>

/**
 * main: prints the string with proper grammar *
 * Description: prints with proper grammar text but the outcome is art
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
